import 'package:flutter/material.dart';

class UserPrifile extends StatelessWidget {
  const UserPrifile();

 @override
  Widget build(BuildContext context) {
    return const Text('TestProfile') ;
  }
}